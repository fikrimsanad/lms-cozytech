import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "LMS CozyTech - Platform Pembelajaran Game Development",
  description:
    "Platform pembelajaran game development terbaik di Indonesia. Belajar Unity, Unreal Engine, 3D Modeling, dan Game Design.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="id">
      <body className="">
        {children}
      </body>
    </html>
  );
}
